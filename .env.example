# Copy this to .env.local and fill in your values

# Neon Postgres — get from neon.tech dashboard
DATABASE_URL="postgresql://user:pass@ep-xxxx.us-east-2.aws.neon.tech/standup?sslmode=require"
DIRECT_URL="postgresql://user:pass@ep-xxxx.us-east-2.aws.neon.tech/standup?sslmode=require"

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="replace-with-a-strong-random-secret"

# Your Vercel URL (set automatically on Vercel, or http://localhost:3000 locally)
NEXTAUTH_URL="http://localhost:3000"
